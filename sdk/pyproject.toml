[project]
name = "vectorspace-sdk"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "anthropic>=0.40",
]

[tool.pytest.ini_options]
pythonpath = ["."]
